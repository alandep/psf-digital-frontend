/**
 * Root package of the EIP platform modular monolith.
 *
 * <p>Each direct sub-package under {@code com.eip} represents a Spring Modulith
 * application module following a hexagonal (ports and adapters) layout. The
 * {@code platform} package holds shared, cross-cutting building blocks and is
 * declared as a shared module in the application bootstrap.
 */
package com.eip;
