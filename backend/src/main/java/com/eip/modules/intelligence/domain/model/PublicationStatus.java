package com.eip.modules.intelligence.domain.model;

/** Publication lifecycle status of a curated intelligence item. */
public enum PublicationStatus {
    SCHEDULED,
    PUBLISHED,
    EXPIRED,
    ARCHIVED
}
